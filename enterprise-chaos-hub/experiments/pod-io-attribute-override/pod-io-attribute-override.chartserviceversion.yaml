---
apiVersion: litmuschaos.io/v1alpha1
kind: ChartServiceVersion
metadata:
  name: pod-io-attribute-override
  version: 0.1.0
  annotations:
    categories: pod-io-attribute-override
    chartDescription: It overrides the attributes of the files present inside the mounted volume. It filters the target pods based on label and namespace selectors.
spec:
  displayName: Pod Io Attribute Override
  categoryDescription: >
    It overrides the attributes of the files present inside the mounted volume. It filters the target pods based on label and namespace selectors.
  faults:
    - name: pod-io-attribute-override
      description: It overrides the attributes of the files present inside the mounted volume. It filters the target pods based on label and namespace selectors.
  keywords:
    - Kubernetes
  plan:
    - enterprise
  platforms:
    - GKE
    - Minikube
  icon:
    - url:
      mediatype: ""
