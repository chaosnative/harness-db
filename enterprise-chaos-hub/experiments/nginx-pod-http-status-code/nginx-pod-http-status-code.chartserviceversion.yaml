---
apiVersion: litmuschaos.io/v1alpha1
kind: ChartServiceVersion
metadata:
  name: nginx-pod-http-status-code
  version: 0.1.0
  annotations:
    categories: nginx-pod-http-status-code
    chartDescription: Injects erroneous status codes in HTTP responses on pod(s) identified by the specified label ("app=nginx" by default)
spec:
  displayName: Nginx Pod HTTP Status Code
  categoryDescription: >
    Injects erroneous status codes in HTTP responses on pod(s) identified by the specified label ("app=nginx" by default)
  faults:
    - name: pod-http-status-code
      description: Injects erroneous status codes in HTTP responses on pod(s) identified by the specified label ("app=nginx" by default)
  keywords:
    - Kubernetes
  plan:
    - free
  platforms:
    - "Minikube"
  icon:
    - url:
      mediatype: ""
