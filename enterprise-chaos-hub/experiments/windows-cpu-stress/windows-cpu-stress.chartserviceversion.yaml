---
apiVersion: litmuschaos.io/v1alpha1
kind: ChartServiceVersion
metadata:
  name: windows-cpu-stress
  version: 0.1.0
  annotations:
    categories: windows-cpu-stress
    chartDescription: Injects CPU stress into a Windows machine.
spec:
  displayName: Windows CPU Stress
  categoryDescription: >-
    This fault injects CPU stress into a Windows machine.

    - Induces CPU consumption and exhaustion on the target Windows machines.

    - Simulates a lack of CPU for processes running on the application, which degrades their performance.

    - Simulates slow application traffic or exhaustion of the resources, leading to degradation in the performance of processes on the machine.
  faults:
    - name: windows-cpu-stress
      description: Injects CPU stress into a Windows machine.
  keywords:
    - Windows
    - GCP
    - Azure
  plan:
    - team
  platforms:
    - Windows
  icon:
    - url:
      mediatype: ""
