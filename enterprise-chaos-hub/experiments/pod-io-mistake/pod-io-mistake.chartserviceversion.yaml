---
apiVersion: litmuschaos.io/v1alpha1
kind: ChartServiceVersion
metadata:
  name: pod-io-mistake
  version: 0.1.0
  annotations:
    categories: pod-io-mistake
    chartDescription: It injects the IO mistake fault, which makes the file read or write a wrong value present inside the mounted volume of the pods. It filters the target pods based on label and namespace selectors.
spec:
  displayName: Pod Io Latency
  categoryDescription: >
    It injects the IO mistake fault, which makes the file read or write a wrong value present inside the mounted volume of the pods. It filters the target pods based on label and namespace selectors.
  faults:
    - name: pod-io-mistake
      description: It injects the IO mistake fault, which makes the file read or write a wrong value present inside the mounted volume of the pods. It filters the target pods based on label and namespace selectors.
  keywords:
    - Kubernetes
  plan:
    - enterprise
  platforms:
    - GKE
    - Minikube
  icon:
    - url:
      mediatype: ""
