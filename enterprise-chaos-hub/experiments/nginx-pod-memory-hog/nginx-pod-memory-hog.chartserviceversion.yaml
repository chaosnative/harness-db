---
apiVersion: litmuschaos.io/v1alpha1
kind: ChartServiceVersion
metadata:
  name: nginx-pod-memory-hog
  version: 0.1.0
  annotations:
    categories: nginx-pod-memory-hog
    chartDescription: Injects memory hog (resource exhaustion) on pod(s) identified by the specified label ("app=nginx" by default)
spec:
  displayName: Nginx Pod Memory Hog
  categoryDescription: >
    Injects memory hog (resource exhaustion) on pod(s) identified by the specified label ("app=nginx" by default)
  faults:
    - name: pod-memory-hog
      description: Injects memory hog (resource exhaustion) on pod(s) identified by the specified label ("app=nginx" by default).
  keywords:
    - Kubernetes
  plan:
    - free
  platforms:
    - GKE
    - Konvoy
    - Packet(Kubeadm)
    - Minikube
    - EKS
    - AKS
    - Kind
    - Rancher
    - OpenShift(OKD)
  icon:
    - url:
      mediatype: ""
