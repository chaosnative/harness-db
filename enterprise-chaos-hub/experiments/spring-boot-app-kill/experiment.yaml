---
kind: Workflow
apiVersion: argoproj.io/v1alpha1
metadata:
  name: spring-boot-app-kill-1658939319
  namespace: litmus

spec:
  templates:
    - name: custom-chaos
      steps:
        - - name: spring-boot-app-kill-t3s
            template: spring-boot-app-kill-t3s
        - - name: cleanup-chaos-resources
            template: cleanup-chaos-resources

    - name: spring-boot-app-kill-t3s
      inputs:
        artifacts:
          - name: spring-boot-app-kill-t3s
            path: /tmp/chaosengine-spring-boot-app-kill-t3s.yaml
            raw:
              data: |
                apiVersion: litmuschaos.io/v1alpha1
                kind: ChaosEngine
                metadata:
                  namespace: "{{workflow.parameters.adminModeNamespace}}"
                  generateName: spring-boot-app-kill-t3s
                  labels:
                    name: spring-boot-app-kill
                    app.kubernetes.io/part-of: litmus
                    app.kubernetes.io/component: experiment-job
                    app.kubernetes.io/version: ci
                spec:
                  appinfo:
                    appns: default
                    applabel: app=spring-boot
                    appkind: deployment
                  # It can be active/stop
                  engineState: 'active'
                  chaosServiceAccount: litmus-admin
                  experiments:
                    - name: spring-boot-app-kill
                      image: artifactory.sdlc.ctl.gcp.db.com/dkr-public-local/com/db/chaos-hce/thirdparty/harness/chaos-go-runner:1.41.0
                      imagePullPolicy: Always
                      args:
                      - -c
                      - ./experiments -name spring-boot-app-kill
                      command:
                      - /bin/bash
                      spec:
                        components:
                          env:
                            # port of the spring boot application
                            - name: CM_PORT
                              value: ''
                             #it contains number of requests are to be attacked
                             # n value means nth request will be affected
                            - name: CM_LEVEL
                              value: '1'
                            # it limits watched packages/classes/methods
                            - name: CM_WATCHED_CUSTOM_SERVICES
                              value: ''
                            # provide name of watcher
                            # it supports controller, restController, service, repository, component, webClient
                            - name: CM_WATCHERS
                              value: 'restController'
                            ## percentage of total pods to target
                            - name: PODS_AFFECTED_PERC
                              value: ''
                            ## Period to wait before and after injection of chaos
                            - name: RAMP_TIME
                              value: ''
                            ## it defines the sequence of chaos execution for multiple target pods
                            ## supported values: serial, parallel
                            - name: SEQUENCE
                              value: 'parallel'
      metadata:
        labels:
          weight: "10"
      container:
        name: ""
        image: artifactory.sdlc.ctl.gcp.db.com/dkr-public-local/com/db/chaos-hce/thirdparty/harness/chaos-go-runner:1.41.0
        command:
          - ./chaos-checker
        args:
          - -file=/tmp/chaosengine-spring-boot-app-kill-t3s.yaml
          - -saveName=/tmp/engine-name

    - name: cleanup-chaos-resources
      container:
        name: ""
        image: artifactory.sdlc.ctl.gcp.db.com/dkr-public-local/com/db/chaos-hce/thirdparty/harness/chaos-go-runner:1.41.0
        command:
          - sh
          - -c
        args:
          - "kubectl delete chaosengine -l 'instance_id in
            (9cf6250c-8173-4c98-b6de-f7147f13e4e2, )' -n
            {{workflow.parameters.adminModeNamespace}} "

  entrypoint: custom-chaos
  arguments:
    parameters:
      - name: adminModeNamespace
        value: litmus
  serviceAccountName: argo-chaos
  podGC:
    strategy: OnWorkflowCompletion
  securityContext:
    runAsUser: 1000
    runAsNonRoot: true
